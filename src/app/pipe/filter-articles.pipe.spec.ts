import { FilterArticlesPipe } from './filter-articles.pipe';

describe('FilterArticlesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArticlesPipe();
    expect(pipe).toBeTruthy();
  });
});
